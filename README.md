# 📝 ToDo App

A modern, intuitive task management application built with React and Vite. Keep track of your daily tasks with a clean and responsive interface.

## ✨ Features

- ✅ **Add Tasks** - Create new tasks with a simple input interface
- ✏️ **Edit Tasks** - Modify existing tasks on the fly
- 🗑️ **Delete Tasks** - Remove completed or unwanted tasks
- ☑️ **Mark Complete** - Toggle task completion status
- 👁️ **Toggle Finished** - Show or hide completed tasks
- 💾 **Local Storage** - Automatically saves tasks to browser storage
- 🎨 **Modern UI** - Clean, responsive design with Tailwind CSS
- 🚀 **Fast & Lightweight** - Built with Vite for optimal performance

## 🛠️ Technologies Used

- **React 19.2.0** - UI library for building user interfaces
- **Vite 7.3.1** - Next-generation frontend build tool
- **Tailwind CSS 4.1.18** - Utility-first CSS framework
- **UUID** - Generate unique IDs for tasks
- **LocalStorage API** - Persistent data storage

## 📋 Prerequisites

Before you begin, ensure you have the following installed:
- Node.js (version 16 or higher)
- npm or yarn package manager

## 🚀 Installation

1. **Clone the repository**
   ```bash
   git clone <repository-url>
   cd "ToDo App"
   ```

2. **Install dependencies**
   ```bash
   npm install
   ```

3. **Start the development server**
   ```bash
   npm run dev
   ```

4. **Open your browser**
   
   Navigate to `http://localhost:5173` (or the port shown in your terminal)

## 🎯 Usage

1. **Adding a Task**: Enter your task in the input field and click "Add Task" (minimum 4 characters required)
2. **Completing a Task**: Click the checkbox next to a task to mark it as complete
3. **Editing a Task**: Click the "Edit" button to modify an existing task
4. **Deleting a Task**: Click the "Delete" button to remove a task
5. **Toggle Finished Tasks**: Use the "Show Finished" checkbox to show/hide completed tasks

## 💡 Features in Detail

### Local Storage Integration
All tasks are automatically saved to your browser's local storage, ensuring your tasks persist even after closing the browser.

### Responsive Design
The application is fully responsive and works seamlessly across desktop, tablet, and mobile devices.
